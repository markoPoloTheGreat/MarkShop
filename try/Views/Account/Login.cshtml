<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MarkShop - Login</title>
    <!-- Importing Google Fonts for that "Handwritten" feel -->
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Dancing+Script:wght@600&family=Lato:wght@400;700&display=swap" rel="stylesheet">
    <style>
        /* --- CORE THEME VARIABLES --- */
        :root {
            --ink-color: #2c3e50;       /* Dark Blue Ink */
            --gold-accent: #c0a16b;     /* Gold Nib Color */
            --paper-color: #fdfbf7;     /* Creamy Paper */
            --paper-shadow: rgba(0, 0, 0, 0.2);
            --line-color: #aeb6bf;      /* Pale blue line rule */
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Lato', sans-serif;
            background-color: #2c3e50;
            background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%2334495e' fill-opacity='0.4'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* --- THE NOTEBOOK CARD --- */
        .login-card {
            background-color: var(--paper-color);
            width: 100%;
            max-width: 400px;
            padding: 40px;
            border-radius: 4px;
            box-shadow:
                10px 10px 20px var(--paper-shadow),
                -1px -1px 0px rgba(255, 255, 255, 0.5); /* Highlight edge */
            position: relative;
            /* Texture for paper */
            background-image: linear-gradient(var(--paper-color) 0.1em, transparent 0.1em), linear-gradient(90deg, var(--paper-color) 0.1em, transparent 0.1em);
            background-size: 2em 2em;
        }

        /* Decorative "Binding" on the left */
        .login-card::before {
            content: '';
            position: absolute;
            left: 20px;
            top: 0;
            bottom: 0;
            width: 2px;
            background-color: #e74c3c; /* Red margin line */
            opacity: 0.6;
        }

        /* --- HEADER & LOGO --- */
        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .nib-icon {
            width: 50px;
            height: 50px;
            fill: var(--gold-accent);
            margin-bottom: 10px;
            filter: drop-shadow(1px 1px 1px rgba(0,0,0,0.3));
        }

        h2 {
            font-family: 'Dancing Script', cursive;
            font-size: 2.5rem;
            color: var(--ink-color);
            margin-bottom: 5px;
        }

        p.subtitle {
            font-family: 'Cinzel', serif;
            font-size: 0.8rem;
            color: #7f8c8d;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        /* --- INPUT FIELDS (Ruled Paper Style) --- */
        .input-group {
            position: relative;
            margin-bottom: 25px;
            padding-left: 30px; /* Space for margin line */
        }

        .input-group input {
            width: 100%;
            padding: 10px 5px;
            font-size: 1.1rem;
            font-family: 'Dancing Script', cursive; /* User types in "handwriting" */
            color: var(--ink-color);
            border: none;
            border-bottom: 2px solid var(--line-color);
            background: transparent;
            outline: none;
            transition: border-color 0.3s;
        }

        /* Placeholder floating logic */
        .input-group label {
            position: absolute;
            top: 10px;
            left: 35px;
            color: #95a5a6;
            font-family: 'Lato', sans-serif;
            font-size: 0.9rem;
            transition: 0.3s ease all;
            pointer-events: none;
        }

        .input-group input:focus {
            border-bottom: 2px solid var(--gold-accent);
        }

        .input-group input:focus ~ label,
        .input-group input:not(:placeholder-shown) ~ label {
            top: -15px;
            left: 30px;
            font-size: 0.75rem;
            color: var(--gold-accent);
            font-family: 'Cinzel', serif;
        }

        /* --- THE INK BUTTON --- */
        .btn-ink {
            position: relative;
            width: calc(100% - 30px);
            margin-left: 30px;
            padding: 12px;
            border: 2px solid var(--ink-color);
            background: transparent;
            color: var(--ink-color);
            font-family: 'Cinzel', serif;
            font-weight: 700;
            cursor: pointer;
            overflow: hidden;
            transition: color 0.4s;
            z-index: 1;
        }

        /* Ink fill effect */
        .btn-ink::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background-color: var(--ink-color);
            transition: transform 0.4s ease-in-out;
            z-index: -1;
            transform: skewX(20deg);
        }

        .btn-ink:hover {
            color: #fff;
        }

        .btn-ink:hover::before {
            transform: translateX(100%) skewX(0deg);
            left: 0;
        }

        /* --- FOOTER LINKS --- */
        .footer-links {
            margin-top: 20px;
            padding-left: 30px;
            text-align: center;
            font-size: 0.85rem;
        }

        .footer-links a {
            color: var(--ink-color);
            text-decoration: none;
            position: relative;
        }

        .footer-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 1px;
            bottom: -2px;
            left: 0;
            background-color: var(--gold-accent);
            transition: width 0.3s;
        }

        .footer-links a:hover::after {
            width: 100%;
        }

        /* --- ERROR MESSAGE --- */
        .alert-box {
            background-color: rgba(231, 76, 60, 0.1);
            border-left: 3px solid #e74c3c;
            color: #c0392b;
            padding: 10px;
            margin-bottom: 20px;
            margin-left: 30px;
            font-size: 0.9rem;
            display: none; /* Hidden by default */
        }

    </style>
</head>
<body>

    <div class="login-card">

        <div class="header">
            <!-- SVG Fountain Pen Nib Icon -->
            <svg class="nib-icon" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path d="M19.07 4.93L17.07 6.93C17.07 6.93 16.03 8.5 13.9 10.63C11.96 12.57 11.23 15.17 12 17.65L10.38 19.27L8.96 17.86C8.86 17.76 8.73 17.76 8.64 17.86L6.5 20L6.71 20.21L2.5 22L4.31 17.81L4.5 18L6.64 15.86C6.73 15.77 6.73 15.63 6.64 15.54L5.23 14.12L6.85 12.5C9.33 13.27 11.93 12.54 13.87 10.6C16 8.47 17.57 7.43 17.57 7.43L19.57 5.43C19.96 5.04 19.96 4.41 19.57 4.03C19.18 3.63 18.55 3.63 18.16 4.03M12.92 11.55C11.66 12.81 10.22 13.31 8.86 13.06L11.44 10.47L13.5 12.53C13.5 12.53 13.28 12.31 13.22 12.25C13.13 12.16 13.13 12.03 13.22 11.94L13.94 11.22C13.62 11.31 13.28 11.42 12.92 11.55Z" />
            </svg>
            <h2>MarkShop</h2>
            <p class="subtitle">Premium Stationery & Goods</p>
        </div>

        <!-- Warning: Make sure asp-action matches your actual controller route -->
        <form action="/Account/Login" method="post">

            <!-- Example Error Box (Logic should be handled by your View) -->
            <!--
            <div class="alert-box">
                Invalid ink credentials. Please check your quill.
            </div>
            -->

            <div class="input-group">
                <input type="email" name="email" id="email" placeholder=" " required autocomplete="off">
                <label for="email">Email Address</label>
            </div>

            <div class="input-group">
                <input type="password" name="password" id="password" placeholder=" " required>
                <label for="password">Password</label>
            </div>

            <button type="submit" class="btn-ink">
                Sign In
            </button>
        </form>

        <div class="footer-links">
            <a href="/Customers/Create">Create New Journal (Register)</a>
        </div>
    </div>

</body>
</html>